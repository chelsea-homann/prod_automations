# 📘 海峰棋院 自動化技能手冊
### Haifong Go Academy — Automation Skills Book（教學與交接版）

> 本手冊將通用的「資料自動化技能」量身改寫成 **棋院日常營運** 的情境：
> 學員與報名資料擷取、賽事編排與成績、問卷與學習回饋、家長／棋士通知等。
> 每一項「技能（Skill）」都包含：**用途說明、對應程式碼、可直接複製的提示語（Prompt）**。
>
> 搭配本專案 `skills/` 資料夾中的實際程式模組使用 —— 把提示語交給 AI 助理
> （例如 Claude），即可產生或調整成棋院自己的自動化流程。

---

## 〇、如何使用本手冊（給交接同仁）

1. **依「功能」找技能**：下表把技能依功能分類，先找到你要做的事（例如「寄通知給家長」）。
2. **看懂三段式內容**：每個技能有 ① 用途 ② 對應程式碼（`skills/` 內的真實模組） ③ 提示語。
3. **複製提示語給 AI 助理**：把 `<尖括號>` 內的占位字換成棋院實際資料（系統網址、欄位名等），貼給 AI 助理即可產生程式。
4. **不要把密碼寫進程式**：所有帳號、密碼、API 金鑰一律放在 `config.env`（見技能 8.x），切勿提交到版本庫。
5. **組合成流程**：每個技能都可獨立替換，照附錄 B 的範例把多個技能串成完整作業。

> 📌 **名詞對照**：本手冊把原本的「人資系統」對應成棋院的**學員管理系統／報名系統**；
> 「問卷平台」對應成棋院的**問卷工具**；「SFTP 上傳」對應成**把名冊／編排表／成績上傳到網站或合作單位**。

| 功能分類 | 涵蓋內容 | 技能 |
|---------|---------|------|
| 1. [資料擷取與彙整](#一資料擷取與彙整) | 從系統抓學員、報名、段級位資料 | 1.1 – 1.4 |
| 2. [資料整理與清理](#二資料整理與清理) | 欄位標準化、篩選、日期格式 | 2.1 – 2.5 |
| 3. [檔案傳輸與發布](#三檔案傳輸與發布) | 上傳名冊／編排／成績到網站或合作單位 | 3.1 – 3.3 |
| 4. [賽事營運](#四賽事營運) | 報名、配對編排、成績、段級位更新 | 4.1 – 4.4 |
| 5. [問卷與學習回饋](#五問卷與學習回饋) | 滿意度／課後回饋的發送與回收 | 5.1 – 5.3 |
| 6. [通知與溝通](#六通知與溝通) | 通知家長、學員、棋士 | 6.1 – 6.3 |
| 7. [排程與自動化](#七排程與自動化) | 讓作業定時、可靠地執行 | 7.1 – 7.4 |
| 8. [設定與資安](#八設定與資安) | 帳密、環境變數、安全 | 8.1 – 8.4 |

---

## 一、資料擷取與彙整
> *把資料從系統「抓出來」。* 將學員名冊、報名資料、段級位／積分匯入成乾淨的表格。
> 對應程式模組：`skills/report_puller.py`

### 1.1 從系統匯出報表（學員名冊、報名清單）
**用途：** 連線到棋院的學員管理／報名系統（任何提供報表網址的系統），下載 CSV 並轉成資料表。
**對應程式：** `skills.report_puller.pull_report()`

```text
請寫一個 Python 函式 pull_report(url, username, password)：
- 使用 requests.Session 與 HTTP Basic 帳密驗證
- 從 <棋院學員管理系統> 的報表網址下載 CSV
- 呼叫 raise_for_status() 並以 UTF-8 解碼
- 以 io.StringIO 轉成 pandas DataFrame 回傳
帳號、密碼、網址一律從環境變數讀取，不可寫死在程式裡。
```

### 1.2 依日期區間抓資料（最近一週報名、本月新生）
**用途：** 在報表網址套入 `{start_dt}`／`{end_dt}`，讓同一支程式可抓任意時間範圍（例如最近 7 天的新報名）。
**對應程式：** `skills.report_puller.date_window()` + `build_report_url()`

```text
幫我的報表程式加上日期區間功能：網址範本含 {start_dt} 與 {end_dt}，
計算 start = 今天 - 回看天數、end = 今天 + 前看天數（皆可用環境變數設定，
格式 YYYY-MM-DD），並套入網址。預設回看 <7> 天、前看 <0> 天。
```

### 1.3 從需等待的匯出 API 取資料（問卷／線上對局平台）
**用途：** 對「先送出申請 → 輪詢進度 → 下載檔案」型的 API（如問卷系統、線上對局平台回報），自動完成整個流程。
**對應程式：** `skills.polling_export.export_responses()`

```text
請實作一個「建立匯出→輪詢進度→下載→解壓縮」的 Python 用戶端：
1. POST 啟動匯出並取得 progressId
2. 反覆 GET 進度端點、印出完成百分比，直到狀態為 complete 或 failed（failed 要丟例外）
3. 依 fileId 下載完成檔，並用 zipfile 在記憶體內解壓
開頭先驗證輸入（格式須為 csv/tsv），以 x-api-token 標頭驗證（金鑰讀環境變數），
並支援選用的 HTTP/HTTPS 代理伺服器。
```

### 1.4 一次抓多份報表（多班別、多分院、多賽事）
**用途：** 用一份 JSON 設定列出多份報表（例如不同班別、不同分院、不同賽事的報名表），程式逐一抓取。新增報表只改設定，不改程式。
**對應程式：** `skills.report_puller.pull_batch()`

```text
把我的多報表程式改成：報表清單來自一個環境變數，內容是 JSON 陣列，例如
[{"name":"週六少兒班","url":"https://...","output":"sat_kids.csv"}]。
逐一抓取每份報表、套用欄位改名、存到 OUTPUT_DIR；
若某份報表的網址為空就略過、不要報錯。
```

---

## 二、資料整理與清理
> *把原始資料變成「可用」。* 報表抓下來後，先標準化、篩選、補欄位再發布。
> 對應程式模組：`skills/transform.py`

### 2.1 欄位名稱標準化（系統欄名 → 中文易讀欄名）
**用途：** 把系統匯出的英文／底線欄名換成棋院慣用的中文欄名；只改存在的欄位，缺欄不會出錯。
**對應程式：** `skills.transform.standardize_columns()`

```text
幫我的資料表加上欄位標準化：在模組最上方放一份改名對照表（系統欄名→中文欄名）。
改名前先過濾出資料表中實際存在的欄位（{k:v for k,v in 對照表 if k in df.columns}），
確保缺欄絕不報錯，且對照表要方便日後擴充。
```

### 2.2 挑選要保留的欄位
**用途：** 把過寬的匯出表縮減成「對外可公開」的欄位子集（例如成績榜只保留姓名、段級位、名次，去掉聯絡資料）。
**對應程式：** `skills.transform.keep_columns()`

```text
加入選用的欄位篩選，由環境變數 COLUMNS_TO_KEEP（JSON 清單）控制：
若有設定，只保留清單中且存在於資料表的欄位（維持順序）；未設定則保留全部。
並印出保留了幾欄。
```

### 2.3 統一日期格式
**用途：** 自動找出含日期的欄位（報名日、開課日、對局日）並統一格式（例如 `YYYY/MM/DD`），無法解析的欄位自動略過。
**對應程式：** `skills.transform.format_dates()`

```text
寫一段處理：掃描欄名含「<日期>」字樣的欄位，用 pd.to_datetime(...).dt.strftime('%Y/%m/%d')
重新格式化，並以 try/except 包住，讓非日期欄位安靜略過。
```

### 2.4 新增衍生／稽核欄位（建檔日、梯次標記）
**用途：** 加上計算欄位，例如「建檔日期」「問卷發送日」「賽事梯次」，並移除被取代的舊欄位。
**對應程式：** `skills.transform.add_stamp()`

```text
資料載入後，新增一欄「<發送日期>」設為今天（格式 YYYY-MM-DD），
若存在舊欄「<原始發送日>」則刪除。欄名與日期格式請設為檔案最上方的常數。
```

### 2.5 略過匯出檔的雜訊表頭列
**用途：** 有些系統匯出的 CSV 前幾列是說明文字，這個技能在讀取時略過指定列號。
**對應程式：** `skills.transform.parse_skip_rows()`

```text
支援環境變數 SKIP_ROWS（逗號分隔的列號，如 "0,2"）。有設定時用
skiprows=lambda x: x in 這些列號、header=0 讀取；未設定則正常讀取。
用來處理「匯出檔含額外說明列」的情況。
```

---

## 三、檔案傳輸與發布
> *把成果送到「該去的地方」。* 上傳名冊、編排表、成績到棋院網站、合作單位或共用磁碟。
> 對應程式模組：`skills/sftp_transfer.py`

### 3.1 透過 SFTP 上傳檔案（名冊／編排表／成績）
**用途：** 用 paramiko 連到 SFTP 伺服器，先移除舊檔再上傳新檔（例如把最新對局編排表送到合作平台）。
**對應程式：** `skills.sftp_transfer.sftp_upload()`

```text
請寫 sftp_upload(host, user, password, remote_dir, local_path, remote_filename)，
使用 paramiko：開啟 SSH（AutoAddPolicy）→ chdir 到 remote_dir →
若遠端已有同名檔先刪除（忽略 FileNotFoundError）→ put 本地檔 → 關閉連線。
所有連線資訊都從環境變數讀取。
```

### 3.2 輸出含日期的 CSV 到共用資料夾
**用途：** 確保輸出資料夾存在，並用含日期的檔名（例如 `成績_2026-06.csv`）避免歷史檔被覆蓋。
**對應程式：** `skills.sftp_transfer.write_dated_csv()`

```text
加入輸出函式：os.makedirs(OUTPUT_DIR, exist_ok=True)，組出檔名
「<前綴>_<YYYY-MM>.csv」，以 df.to_csv(path, index=False, na_rep='') 寫出，並印出路徑。
```

### 3.3 先暫存再清理
**用途：** 先把檔案寫到暫存資料夾再傳輸，成功交付後自動刪除本地暫存檔，避免敏感名冊殘留。
**對應程式：** `skills.sftp_transfer.staged_file()`（context manager）

```text
把我的傳輸作業改成：用 tempfile 建立暫存資料夾寫出 CSV → 上傳 →
上傳成功後才刪除本地暫存。失敗時保留檔案，並把它附在失敗通知信中。
```

---

## 四、賽事營運
> *棋院的核心。* 從報名、配對編排、成績到段級位更新的整條流程。海峰盃等賽事皆適用。
> 對應程式模組：`skills/report_puller.py` + `transform.py` + `sftp_transfer.py`（組合運用）

### 4.1 彙整賽事報名名單
**用途：** 抓取某場賽事（如海峰盃、少兒梯隊賽）的報名資料，標準化欄位、加上梯次標記，產出乾淨的參賽名冊。
**對應程式：** 1.1 + 1.2 + 2.1 + 2.4 組合

```text
做一支「賽事報名彙整」程式：
1. 從報名系統抓取 <海峰盃> 在最近 <回看天數> 的報名資料
2. 欄位改名為標準參賽名冊格式（姓名、段級位、聯絡方式、組別）
3. 新增「賽事名稱」與「報名日期」欄位
4. 輸出含日期的 CSV，並寄出完成通知；失敗時寄錯誤通知
```

### 4.2 產生對局配對／編排表
**用途：** 依賽制（**單淘汰**＝海峰盃形式、**瑞士制**、**循環賽**）由參賽名冊產生每輪配對表。
**對應程式：** 新增 `tournament.py`（可用下方提示語產生）

```text
請寫一個圍棋賽事配對模組，輸入是含「姓名、段級位、積分」的參賽 DataFrame，
支援三種賽制：
- single_elim（單淘汰，海峰盃形式）：依種子序產生對戰籤表
- swiss（瑞士制）：同分相遇、避免重複對手，輸出本輪配對
- round_robin（循環賽）：產生完整循環賽程
每輪輸出含「桌號、黑方、白方、讓子」的編排表 DataFrame，方便用技能 3.x 發布。
```

### 4.3 處理對局成績
**用途：** 收回每輪結果，計算勝負、累積積分與名次，產出成績榜。
**對應程式：** `transform.py` + 新增成績彙總邏輯

```text
寫一段成績處理：輸入每輪結果（桌號、黑方、白方、勝方），
更新每位棋手的勝場、積分與對手分（SOS），依規則排序產生名次榜 DataFrame。
保留可公開欄位（姓名、段級位、勝場、名次），用技能 2.2 篩欄後輸出。
```

### 4.4 更新段級位／積分
**用途：** 依賽事結果調整學員的段級位或等級分，回寫學員系統並保留變動紀錄。
**對應程式：** `report_puller.py`（讀） + `transform.py`（算） + `sftp_transfer.py`（回寫）

```text
做一支段級位更新程式：
1. 讀取現行學員段級位／等級分
2. 套用本次賽事的升降規則（例如連勝 N 場升級、等級分依勝負對手計算）
3. 產生「異動清單」（學員、原級位、新級位、原因）供存查
4. 把更新後名冊輸出並上傳回系統；寄出異動摘要通知
```

---

## 五、問卷與學習回饋
> *傾聽家長與學員。* 發送滿意度／課後回饋問卷，並把回收結果整理交付。
> 對應程式模組：`skills/polling_export.py` + `transform.py` + `email_notification.py`

### 5.1 產生並上傳問卷名單
**用途：** 抓取應收問卷的對象（例如本期結業學員、家長），整理成問卷平台需要的匯入格式並上傳。
**對應程式：** 1.1 + 2.1 + 3.1 組合

```text
做一支「問卷對象名單」程式：抓取 <本期學員/家長> 名單 → 改名為問卷平台匯入格式
→ 寫暫存 CSV → 上傳到問卷平台的收件資料夾 → 寄出完成通知，失敗時寄錯誤（附檔）。
```

### 5.2 匯出問卷回覆
**用途：** 用問卷平台的回覆匯出 API（建立→輪詢→下載→解壓）取回填答結果成 CSV。
**對應程式：** `skills.polling_export.export_responses()` + `transform.py`

```text
用「輪詢匯出」用戶端匯出問卷回覆（useLabels=true）→ 解壓 → 讀第一個 .csv 成 DataFrame
→ 交給清理流程（技能 2.2–2.5）。問卷 ID、資料中心、API 金鑰皆讀環境變數。
```

### 5.3 把回饋結果交付給負責老師
**用途：** 將整理好的回饋結果存檔，並以附件 email 寄給授課老師或主管。
**對應程式：** `email_notification.send_email()`（既有模組）

```text
匯出並整理問卷回饋後，存成含日期的 CSV；若有設定 EMAIL_RECIPIENTS，
就以附件寄出，主旨「<YYYY-MM> 學員回饋彙整」。未設收件人則安靜略過。
```

---

## 六、通知與溝通
> *讓人知道結果。* 用 email 通知家長、學員、棋士；讓自動作業不會「默默失敗」。
> 對應程式模組：`email_notification.py`（既有模組）

> 💡 **棋院延伸：** 若日後要改用 **LINE 官方帳號／LINE Notify** 通知家長，可沿用技能 6.2
> 的「成功／失敗」骨架，只把 `send_email` 換成 LINE 推播函式即可（見提示語）。

### 6.1 寄送 Email（可帶附件）
**用途：** 可重複使用的 `send_email()`，支援純文字內容與選用附件（成績榜、編排表、回饋彙整）。
**對應程式：** `email_notification.send_email()`

```text
寫一個可重複使用的 send_email(sender, recipients, subject, body, smtp_host=None,
smtp_port=None, attachment_name=None, attachment_path=None)，用 smtplib 與 email.mime。
未指定時改讀 SMTP_HOST/SMTP_PORT 環境變數（預設埠 25）。
若要附件，以 MIMEBase 八位元串流做 base64 編碼；找不到檔案要丟 FileNotFoundError。
```

### 6.2 作業成功／失敗通知
**用途：** 每個步驟用 try/except 包住，成功寄「完成」、失敗寄「失敗（含錯誤訊息）」給負責同仁。
**對應程式：** 各自動化腳本共用的 try/except → send_email 模式

```text
把作業的每個步驟用 try/except 包住：成功寄「<作業名> 完成」通知；
失敗寄「<作業名> 失敗」通知，內含步驟名與例外訊息，然後以非零碼結束。
共用我的 send_email 函式。
```

### 6.3 失敗時附上產出檔
**用途：** 當「上傳／發布」步驟失敗時，把產出的檔案附在通知信，讓同仁可手動補送，不必整批重跑。
**對應程式：** SFTP 失敗分支的 send_email（附件）

```text
專門針對傳輸失敗：把當下要交付的檔案附在失敗通知信
（attachment_name + attachment_path），讓同仁無需重跑即可手動補送。

（延伸）若改用 LINE Notify：寫 notify_line(token, message) 以 POST 到
https://notify-api.line.me/api/notify，token 讀環境變數；在成功/失敗分支呼叫它。
```

---

## 七、排程與自動化
> *可靠、準時地跑。* 把腳本包成穩定的定時作業。
> 對應程式：`run_automation.bat` + `skills/retry.py`

### 7.1 通用執行器（含紀錄檔）
**用途：** 一支包裝程式：啟用正確環境、依名稱執行任一腳本、把輸出寫進各自的紀錄檔並回報成功／失敗。
**對應程式：** `run_automation.bat`

```text
寫一支通用執行器（<.bat / shell>）：啟用環境變數指定的 <conda> 環境，
以 $1 接收腳本檔名、用 python 執行，將 stdout+stderr 導向 logs/<腳本>_log.txt，
並依結束碼印出 [SUCCESS]/[FAILED]。
```

### 7.2 設定定時作業
**用途：** 把執行器登錄到作業系統排程（Windows 工作排程器／cron），每日或每月自動跑。
**對應程式：** `run_automation.bat` 的預期用法

```text
給我 <Windows 工作排程器 / cron> 的設定，讓 run_automation.bat <腳本.py>
在 <每天早上 6 點 / 每月 1 號> 執行，包含完整指令列、工作目錄、以及紀錄檔保存方式。
```

### 7.3 結束碼紀律
**用途：** 確保腳本失敗時回非零碼，排程器與執行器才能偵測並反應。
**對應程式：** 失敗分支的 `sys.exit(1)`

```text
檢查我的腳本結束碼是否正確：所有無法復原的失敗路徑都要在通知後 sys.exit(1)，
正常路徑回 0。指出任何「吞掉錯誤卻沒結束」的 except 區塊。
```

### 7.4 網路重試與退避
**用途：** 對不穩定的網路操作（抓報表、SFTP、寄信）加上指數退避重試，避免偶發斷線害整批失敗。
**對應程式：** `skills.retry.retry()` / `with_retry()`

```text
加上指數退避的重試輔助（如 2s、4s、8s、16s；最多 <4> 次），套用到網路操作——
報表下載、SFTP 上傳、SMTP 寄信。只對暫時性／連線錯誤重試，
驗證失敗或 4xx 用戶端錯誤則立即放棄。
```

---

## 八、設定與資安
> *天生安全。* 妥善處理帳密、環境與校園網路現實。學員與家長資料務必謹慎。
> 對應程式：`config.env.example`

### 8.1 所有設定外置到環境變數
**用途：** 把每個帳密、網址、參數移到環境變數，並用提交版的 `config.env.example` 紀錄（絕不提交填好的 `config.env`）。
**對應程式：** `config.env.example`

```text
把我腳本裡所有寫死的值（帳密、網址、主機、輸出路徑、日期區間）改成讀 os.environ，
合理者用 .get() 給預設值。產生一份 config.env.example 逐項註解每個變數，
並把 config.env 加進 .gitignore。絕不提交真實機密。
```

### 8.2 必填 vs. 選填變數
**用途：** 缺少「必填」機密時立即明確報錯；「選填」設定則給預設值。
**對應程式：** `os.environ['X']`（必填）vs. `os.environ.get('X', 預設)`（選填）

```text
把我的環境變數分成必填（用 os.environ['X']，缺少就在啟動時立即報錯）與
選填（os.environ.get('X', 預設)）。並在檔案最上方列出哪些屬於哪一類。
```

### 8.3 校園／企業網路代理
**用途：** 在受限網路下，為對外連線加上選用的 HTTP/HTTPS 代理設定。
**對應程式：** `skills.polling_export` 的 `proxies` 參數

```text
加入選用代理支援：若有設定 PROXY_HTTP 或 PROXY_HTTPS 環境變數，
組成 proxies 字典傳給每個 requests 呼叫；否則傳 proxies=None。
```

### 8.4 機密與個資衛生
**用途：** 讓機密與**學員／家長個資**不出現在原始碼、紀錄檔與版本庫；並記錄各平台的載入方式。
**對應程式：** `config.env.example` 開頭的指引

```text
檢查我的專案的機密與個資衛生：確認原始碼、紀錄檔、提交檔案中沒有任何帳密或個資；
確認 config.env 已被 gitignore；並說明在 <Windows .bat SET 包裝 / Linux source config.env>
如何載入機密。標出任何可能把機密或學員個資印到紀錄檔的地方。
```

---

## 附錄 A — 技能對應程式碼

| 技能 | 對應檔案 |
|------|---------|
| 1.1, 1.2, 1.4 | `skills/report_puller.py` |
| 1.3, 5.2 | `skills/polling_export.py` |
| 2.1–2.5 | `skills/transform.py` |
| 3.1–3.3 | `skills/sftp_transfer.py` |
| 4.1–4.4 | `report_puller.py` + `transform.py` + `sftp_transfer.py`（＋自建 `tournament.py`） |
| 5.3, 6.1, 6.2, 6.3 | `email_notification.py` |
| 7.1–7.3 | `run_automation.bat` |
| 7.4 | `skills/retry.py` |
| 8.1, 8.2, 8.4 | `config.env.example` |

## 附錄 B — 把技能組成完整流程

**範例一：每週賽事報名彙整與發布**
```
1.2 日期區間 → 1.1 抓報名 → 2.1 欄位改名 → 2.4 加梯次標記
   → 4.2 產生配對編排 → 3.3 暫存 → 3.1 SFTP 上傳網站 → 3.3 清理 → 6.2 成功／失敗通知
```

**範例二：學期末問卷回收與交付**
```
5.1 上傳問卷名單 → 5.2 輪詢匯出回覆 → 2.5 略過雜訊列 → 2.2 篩選欄位
   → 2.3 統一日期 → 3.2 輸出含日期 CSV → 5.3 寄給授課老師
```

**範例三：賽後段級位更新**
```
1.1 讀現行段級位 → 4.3 處理成績 → 4.4 套升降規則 → 3.1 回寫系統 → 6.2 寄出異動摘要
```

> 每個編號技能都可獨立替換：換資料來源（1.x）、換整理方式（2.x）、換交付管道（3.x），其餘不動。

---

## 附錄 C — 給教學者的交接重點

- **先示範一條完整流程**（建議用附錄 B 範例一），讓接手同仁看見「抓→整理→發布→通知」全貌。
- **強調資安**（第八章）：學員與家長個資不可外流，密碼一律放 `config.env`。
- **示範改一個提示語**：把某技能的 `<占位字>` 換成真實值，貼給 AI 助理產生程式，建立信心。
- **建立小型清單**：把棋院實際要自動化的作業（報名、編排、成績、問卷、通知）逐項對到本手冊技能編號。
- **延伸方向**：賽事配對 `tournament.py`（技能 4.2）與 LINE 通知（技能 6.3 延伸）是最值得棋院自建的兩塊。

---

*本手冊為 海峰棋院 量身改寫，基礎來自 `prod_automations` 自動化工具組與其 `SKILLS_BOOK.md`。*
*新增技能時，請放到對應「功能」下，附上：一句用途、對應程式、可複製的提示語。*
